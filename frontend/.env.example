# Optional: Set this to your deployed backend URL if frontend is hosted separately (e.g. Vercel / Netlify / Render)
# If left unset, it automatically uses 'http://localhost:8000/api' in local dev or '/api' when behind Nginx reverse proxy
VITE_API_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/api/stream/live
