# Database configuration (defaults to SQLite, can be PostgreSQL in production)
DATABASE_URL=sqlite:///./ulpf_database.sqlite
# DATABASE_URL=postgresql://user:password@localhost:5432/ulpf_db

PORT=8000
HOST=0.0.0.0
